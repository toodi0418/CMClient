{
  "schemaVersion": 1,
  "releaseVersion": "2.0.0-rc.1",
  "requiredTargets": [
    "darwin-aarch64",
    "darwin-x86_64",
    "linux-aarch64",
    "linux-x86_64",
    "windows-x86_64"
  ],
  "requiredModes": [
    "artifact",
    "desktop-portable",
    "desktop-native",
    "headless",
    "cli",
    "systemd",
    "launchd",
    "windows-service",
    "docker",
    "meshtastic-tcp",
    "meshtastic-serial",
    "aprs",
    "callmesh",
    "proxy",
    "management-web",
    "remote-control",
    "backup",
    "migration",
    "update-rollback",
    "security"
  ],
  "cases": [
    {
      "id": "RC-ARTIFACT-IDENTITY",
      "gate": "rc",
      "required": true,
      "validator": "machine",
      "targets": ["all"],
      "modes": ["artifact"],
      "procedure": "Bind the source commit, tree, workflow runs, release index, artifact names, sizes, digests, and expiry to one RC evidence record.",
      "expected": "Every deliverable reports 2.0.0-rc.1 and resolves to the same immutable source commit.",
      "evidence": ["CI run URL", "Release Build Matrix URL", "release-index.json SHA-256"]
    },
    {
      "id": "RC-SUPPLY-CHAIN",
      "gate": "rc",
      "required": true,
      "validator": "machine",
      "targets": ["all"],
      "modes": ["artifact"],
      "procedure": "Verify SHA256SUMS, the release index, canonical build manifests, SPDX SBOM, native installers, and Docker OCI subjects.",
      "expected": "All expected subjects exist exactly once and digest, size, composition, file type, and version checks pass.",
      "evidence": ["SHA256SUMS", "SPDX SBOM", "release verification log"]
    },
    {
      "id": "RC-DESKTOP-PORTABLE",
      "gate": "rc",
      "required": true,
      "validator": "human",
      "targets": ["all"],
      "modes": ["desktop-portable"],
      "procedure": "Extract the exact Desktop portable archive, provide the supported Node runtime, start Agent and Desktop, then exercise single-instance, tray, Web toggle, restart, and exit.",
      "expected": "The Desktop remains a small Agent client, the second instance focuses the first, and user state survives refresh and removal of the old release directory.",
      "evidence": ["archive SHA-256", "host OS and Node version", "sanitized lifecycle transcript"]
    },
    {
      "id": "RC-DESKTOP-NATIVE",
      "gate": "rc",
      "required": true,
      "validator": "human",
      "targets": ["all"],
      "modes": ["desktop-native"],
      "procedure": "Install or mount the target-native Desktop package, inspect bundled runtime resources, launch the GUI, then uninstall without deleting external CMClient state.",
      "expected": "The native package launches on its declared architecture, contains the matching portable runtime resources, and does not claim to auto-register Agent services.",
      "evidence": ["installer SHA-256", "installed package identity", "sanitized launch and uninstall record"]
    },
    {
      "id": "RC-HEADLESS-HOST",
      "gate": "rc",
      "required": true,
      "validator": "human",
      "targets": ["all"],
      "modes": ["headless"],
      "procedure": "Extract Headless, verify Node on the service account PATH, run version and config checks, start Agent/Gateway/Web, and stop cleanly.",
      "expected": "Control API remains available independently, Gateway health becomes ready, and all binaries report the RC version.",
      "evidence": ["archive SHA-256", "Node version", "bounded health and shutdown transcript"]
    },
    {
      "id": "RC-CLI-HOST",
      "gate": "rc",
      "required": true,
      "validator": "human",
      "targets": ["all"],
      "modes": ["cli"],
      "procedure": "Run the standalone CLI version, status, doctor, events snapshot, and an expected failing command against a local Agent.",
      "expected": "The CLI reports 2.0.0-rc.1, uses only Control API, preserves stable exit codes, and prints no secret values.",
      "evidence": ["CLI archive SHA-256", "exit-code table", "sanitized command transcript"]
    },
    {
      "id": "RC-SYSTEMD-HOST",
      "gate": "rc",
      "required": true,
      "validator": "human",
      "targets": ["linux-aarch64", "linux-x86_64"],
      "modes": ["systemd"],
      "procedure": "Install, start, restart, upgrade the Agent path, and uninstall the real systemd unit using a non-login account with Node on PATH.",
      "expected": "The unit runs the exact RC path and uninstall retains configuration, data, cache, and logs.",
      "evidence": ["unit status", "service account PATH proof", "retained-state digest"]
    },
    {
      "id": "RC-LAUNCHD-HOST",
      "gate": "rc",
      "required": true,
      "validator": "human",
      "targets": ["darwin-aarch64", "darwin-x86_64"],
      "modes": ["launchd"],
      "procedure": "Install, bootstrap, restart, update the Agent path, and uninstall the real launchd plist with Node visible to launchd.",
      "expected": "The exact RC starts after login and plist removal retains Application Support, cache, and logs.",
      "evidence": ["launchctl status", "Node path proof", "retained-state digest"]
    },
    {
      "id": "RC-WINDOWS-SERVICE-HOST",
      "gate": "rc",
      "required": true,
      "validator": "human",
      "targets": ["windows-x86_64"],
      "modes": ["windows-service"],
      "procedure": "Install the final Service archive as LocalService, start it, update its binary path, restart it, and uninstall it.",
      "expected": "Service Host supervises the adjacent RC Agent, Node is available to the account, and ProgramData state survives SCM deletion.",
      "evidence": ["service qc/status", "service account Node proof", "retained-state digest"]
    },
    {
      "id": "RC-DOCKER-OCI",
      "gate": "rc",
      "required": true,
      "validator": "human",
      "targets": ["linux-aarch64", "linux-x86_64"],
      "modes": ["docker"],
      "procedure": "Load the checksummed OCI image, run the constrained three-service compose topology, force-recreate it, and verify the named-volume sentinel.",
      "expected": "The image digest matches release metadata; only Ingress publishes loopback; Agent, CLI, Desktop, Service Host, serial, and self-update remain absent.",
      "evidence": ["OCI SHA-256", "image config digest", "sanitized compose smoke log"]
    },
    {
      "id": "RC-MESHTASTIC-TCP",
      "gate": "rc",
      "required": true,
      "validator": "human",
      "targets": ["all"],
      "modes": ["meshtastic-tcp"],
      "procedure": "Connect to a controlled Meshtastic TCP simulator, replay sanitized packets, interrupt the link, and observe bounded reconnect and deduplication.",
      "expected": "Frames decode deterministically, network/node state stays isolated, and reconnect does not duplicate durable domain records.",
      "evidence": ["fixture digest", "stable connection states", "bounded record counts"]
    },
    {
      "id": "RC-MESHTASTIC-SERIAL",
      "gate": "rc",
      "required": true,
      "validator": "hardware",
      "targets": ["darwin-aarch64", "darwin-x86_64", "linux-aarch64", "linux-x86_64", "windows-x86_64"],
      "modes": ["meshtastic-serial"],
      "procedure": "Use an approved physical radio to validate enumeration, exclusive ownership, configure, reconnect, unplug, and replug behavior.",
      "expected": "Only Agent/Gateway owns the device, disconnects use stable states, and no raw packet or device identifier enters evidence.",
      "evidence": ["hashed device identity", "stable state sequence", "sanitized reconnect timings"]
    },
    {
      "id": "RC-APRS-IGATE",
      "gate": "rc",
      "required": true,
      "validator": "hardware",
      "targets": ["all"],
      "modes": ["aprs"],
      "procedure": "With an approved test callsign, replay multi-iGate ordering cases and verify one real precision-32 APRS-IS TX/RX plus disconnect retry.",
      "expected": "Identical source events create byte-identical APRS Data; stale, ambiguous, conflicting, future, or precision-below-32 positions never upload.",
      "evidence": ["hashed test callsign", "APRS Data digest", "stable outbox/monitor states"]
    },
    {
      "id": "RC-CALLMESH-TENANT",
      "gate": "rc",
      "required": true,
      "validator": "hardware",
      "targets": ["all"],
      "modes": ["callmesh"],
      "procedure": "Use an approved test tenant to validate heartbeat, provision, version ordering, conflict handling, key rotation, and transient retry.",
      "expected": "Only verified conflict-free mappings become active and credentials remain in Agent-owned secret storage.",
      "evidence": ["tenant alias hash", "mapping version/count", "stable result codes"]
    },
    {
      "id": "RC-PROXY-MULTICLIENT",
      "gate": "rc",
      "required": true,
      "validator": "hardware",
      "targets": ["all"],
      "modes": ["proxy"],
      "procedure": "Connect multiple real or lab clients, exercise config caching, serialized writes, ACK ownership, slow-client pressure, disconnects, and LAN allowlists.",
      "expected": "Sessions remain protocol-aware and isolated; no client can steal an ACK or stall unrelated clients.",
      "evidence": ["hashed client identities", "session/capacity counts", "stable rejection codes"]
    },
    {
      "id": "RC-MANAGEMENT-WEB",
      "gate": "rc",
      "required": true,
      "validator": "human",
      "targets": ["all"],
      "modes": ["management-web"],
      "procedure": "Exercise loopback and LAN HTTPS login, session expiry, CSRF/origin rejection, rate limits, Web disable, and internal Control listener continuity.",
      "expected": "LAN access is authenticated and bounded; disabling Management Web never disables local Control API.",
      "evidence": ["sanitized browser record", "stable HTTP/error codes", "Control status after Web disable"]
    },
    {
      "id": "RC-REMOTE-CONTROL",
      "gate": "rc",
      "required": true,
      "validator": "human",
      "targets": ["all"],
      "modes": ["remote-control"],
      "procedure": "Use a scoped HTTPS token to run a permitted CLI command, then replay and alter signed requests.",
      "expected": "Valid scope succeeds; replay, expiry, method/path/body changes, and oversized SSE lines fail with stable codes.",
      "evidence": ["token alias only", "stable exit/error codes", "sanitized replay transcript"]
    },
    {
      "id": "RC-BACKUP-RESTORE",
      "gate": "rc",
      "required": true,
      "validator": "human",
      "targets": ["all"],
      "modes": ["backup"],
      "procedure": "Create data, run integrity and backup Jobs, restore an isolated copy, and exercise retention while SSE clients are connected.",
      "expected": "WAL-safe backup verifies, Job progress remains durable and bounded, and restored data passes integrity checks.",
      "evidence": ["database/backup digest", "Job IDs and terminal codes", "row-count summary"]
    },
    {
      "id": "RC-LEGACY-MIGRATION",
      "gate": "rc",
      "required": true,
      "validator": "human",
      "targets": ["all"],
      "modes": ["migration"],
      "procedure": "Use a de-identified Legacy copy for dry-run, apply, verification, repeat/idempotency, tamper rejection, and rollback.",
      "expected": "Only supported settings/history migrate; source changes or backup tampering fail closed; rollback restores the bound target.",
      "evidence": ["source manifest digest", "bounded row-count report", "rollback verification code"]
    },
    {
      "id": "RC-UPDATE-ROLLBACK",
      "gate": "rc",
      "required": true,
      "validator": "human",
      "targets": ["all"],
      "modes": ["update-rollback"],
      "procedure": "In an isolated data copy, exercise authenticated RC staging, digest/signature rejection, health failure rollback, interruption recovery, and status SSE.",
      "expected": "Agent alone owns update, failed health restores the previous release and data, and restart resumes or rolls back from the durable journal.",
      "evidence": ["test manifest digest", "phase/code timeline", "pre/post data digest"]
    },
    {
      "id": "RC-SECURITY-MACHINE",
      "gate": "rc",
      "required": true,
      "validator": "machine",
      "targets": ["all"],
      "modes": ["security"],
      "procedure": "Run dependency, signature, secret, RustSec, workflow-policy, load, E2E, and platform smoke gates on the exact RC commit.",
      "expected": "CI and Release Build Matrix both complete successfully with no skipped required job or exposed credential.",
      "evidence": ["CI run URL", "Release Build Matrix URL", "security and load job IDs"]
    },
    {
      "id": "RC-RASPBERRY-PI",
      "gate": "rc",
      "required": true,
      "validator": "hardware",
      "targets": ["linux-aarch64"],
      "modes": ["headless", "systemd"],
      "procedure": "Run the final Linux ARM64 Headless archive on a Raspberry Pi ARM64 host, install systemd, restart, and exercise TCP or Serial transport.",
      "expected": "The Ubuntu 22.04 glibc baseline, Node prerequisite, service lifecycle, and selected transport work on physical ARM64 hardware.",
      "evidence": ["hashed host identity", "OS/CPU/Node versions", "sanitized service and transport record"]
    },
    {
      "id": "PROD-CHECKSUM-SIGNATURE",
      "gate": "production",
      "required": true,
      "validator": "human",
      "targets": ["all"],
      "modes": ["artifact", "security"],
      "procedure": "After P12-T05 approval, dispatch the exact immutable version tag through the protected production-release environment with attestation enabled, after the platform-signed finalizer has completed.",
      "expected": "Cosign verifies the finalized SHA256SUMS signature against the exact workflow identity and tag.",
      "evidence": ["tag commit", "production workflow run URL", "platform-signed artifact digest", "Sigstore bundle verification"]
    },
    {
      "id": "PROD-PLATFORM-SIGNING",
      "gate": "production",
      "required": true,
      "validator": "human",
      "targets": ["all"],
      "modes": ["desktop-native"],
      "procedure": "After P12-T05 approval, dispatch the exact immutable version tag through the protected production-release environment. For each target, sign the prebuilt native Desktop bytes with Apple codesign plus notarization and staple on Darwin, Authenticode plus a trusted timestamp on Windows, or an embedded GPG signature on Linux AppImage verified by the checksum-pinned AppImageUpdate validator; keep Linux DEB checksum/provenance-bound. Verify the platform trust result, emit the canonical receipt, overlay all five target outputs, regenerate the SBOM, release index, and SHA256SUMS, and run verify --require-platform-signing.",
      "expected": "Every native Desktop package and receipt resolves to the approved source commit, signed bytes differ from the unsigned base where platform trust applies, and the finalized artifact passes required platform-signing verification without exposing signing material.",
      "evidence": ["signed package SHA-256", "platform-signing receipt set", "platform trust verification", "notarization or signing identity reference", "finalized release index and SHA256SUMS"]
    },
    {
      "id": "PROD-PROVENANCE",
      "gate": "production",
      "required": true,
      "validator": "human",
      "targets": ["all"],
      "modes": ["artifact", "security"],
      "procedure": "After P12-T05 approval and platform-signing finalization, verify GitHub provenance subjects against every checksummed archive, installer, platform-signing receipt, OCI image, and SBOM.",
      "expected": "All production subjects bind to the approved tag workflow with no unchecksummed deliverable.",
      "evidence": ["attestation URL", "subject digest set", "verification transcript"]
    },
    {
      "id": "PROD-UPDATE-MANIFEST",
      "gate": "production",
      "required": true,
      "validator": "human",
      "targets": ["all"],
      "modes": ["artifact", "update-rollback"],
      "procedure": "After P12-T05 approval, platform-signing verification, and provenance verification, create the signed Agent update manifest for the immutable HTTPS archive directory.",
      "expected": "The manifest is created only from the attested platform-signed index, contains only updater-managed portable archives, validates the expected key ID, and installs through the Agent health/rollback gate.",
      "evidence": ["manifest SHA-256", "signing key ID", "sanitized verification and update result"]
    }
  ]
}
